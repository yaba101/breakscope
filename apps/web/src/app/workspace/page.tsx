import { BreakscopeWorkspace } from "@/components/breakscope-workspace";

export const metadata = { title: "Stress test workspace" };

export default function WorkspacePage() { return <BreakscopeWorkspace />; }
